<?php

/**
 * @file
 */

use Drupal\Core\Access\AccessResult;
use Drupal\Core\Form\FormStateInterface;
use Drupal\Core\Url;
use Drupal\taxonomy\Entity\Vocabulary;

/**
 * Implements __hook_theme().
 */
function example_ajax_form_theme($existing, $type, $theme, $path) {
  return [
    'exampleajaxform' => [
      'template' => 'exampleajaxform',
      'render element' => 'form',
    ],
  ];
}
